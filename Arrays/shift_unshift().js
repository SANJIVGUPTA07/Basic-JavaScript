let arr = [1,2,3,4,5];
console.log("element remove is:" +arr.shift()); //remove the first element and returns the array
console.log(arr);

console.log("added item and size of array is now "+arr.unshift(1)); //it add at first and return the new size of arr
console.log(arr);
